interface EventProps {
}

const Event = ({}: EventProps) => {
    return (
        <>
        <div className="h-screen">
            Events
        </div>
           
        </>
    );
};

export default Event;